{
  "name": "halwani-market-visit",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css app.js manifest.json assets dist/"
  }
}
